
// это старое

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     output: "export",
//     distDir: "build"
// };

// export default nextConfig;

// новое

/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: "build"
};

export default nextConfig;
